import Locations from './locations';
import LogicCalculation from './logic-calculation';

export default class Spheres {
  static calculate(trackerState) {
    return Locations.mapLocations(() => null);
  }
}
